name: CI Pipeline

on:

 push:

   branches:
      - main


jobs:

 build:

   runs-on: ubuntu-latest

   steps:

   - uses: actions/checkout@v2


   - name: install packages

     run: |

       pip install -r requirements.txt


   - name: train model

     run: |

       python src/train.py
